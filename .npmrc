# Fail the install on an unsupported Node rather than at test time. Without
# this, running the suite on Node 20 crashes deep inside undici with
# "webidl.util.markAsUncloneable is not a function", which says nothing about
# the actual cause (jsdom 30 and its dependencies require Node 22+).
engine-strict=true
